#include <stdio.h>

#define MAX 100

int main()
{
    int n, a[MAX], i, sum, curP, curQ;
    
    scanf("%d", &n);
    
    for (i = 0; i < n; ++i)
    {
        scanf("%d", &a[i]);
    }
    
    sum = 0;
    curP= 0;
    for (i = 0; i < n; ++i)
    {
        sum += a[i];
        if (a[i] < 0 &&)
        {

        }
    }

    
    return 0;
}